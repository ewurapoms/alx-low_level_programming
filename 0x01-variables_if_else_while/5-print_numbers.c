#include <stdio.h>

/**
 * main - Entry point
 * Description: playing with base 10 numbers 
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	while (n < 10)
		printf("%d, n");
	n += 1;
	printf('\n');
	return (0);
}
