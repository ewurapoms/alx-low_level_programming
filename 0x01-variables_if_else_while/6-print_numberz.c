#include <stdio.h>

/**
 * main - Entry point
 * Description:output base 10 number with putchar
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b;

	for
		(b = 0; b <= 9;)
		 b++;
			putchar(b + 48);
	putchar('\n');
	return (0);
}
