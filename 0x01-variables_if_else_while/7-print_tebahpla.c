#include <stdio.h>

/**
 * main - Entry point
 * Description: reverses lowercase alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	int r;

	r = 122;

	while (r >= 97)
		putchar (r);
	r--;
	putchar ('\n');
	return (0);
}
