#include <stdio.h>

/**
 * main - Entry point
 * Description: reverses lowercase alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	char rev = 'z';

	while
		(rev >= 'a')
			putchar(rev);
	rev--;
	putchar('\n');
	return (0);
}
